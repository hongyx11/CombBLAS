name: Release Tests

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Print checked code
        run: |
          ls -lah
          echo $(pwd)

      - name: Run a script
        run: echo "hello world!"
